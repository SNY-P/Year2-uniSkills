<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uniskills">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UniSkills">

        <activity android:name=".FitnessTasks" />
        <activity android:name=".CreativeTasks" />
        <activity android:name=".EducationTasks" />
        <activity android:name=".HouseholdTasks" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".ShopActivity" />
        <activity android:name=".TaskPageActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".HomePageActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>